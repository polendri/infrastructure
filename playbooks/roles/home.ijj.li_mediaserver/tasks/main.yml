---
- name: "Configure 'mediaserver' service"
  tags:
    - deploy
    - update
  block:
    - import_tasks: "configure.yml"
- name: "Deploy 'mediaserver' service"
  block:
    - import_tasks: "deploy.yml"
