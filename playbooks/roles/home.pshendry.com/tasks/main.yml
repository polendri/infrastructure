---
- name: "Configure system"
  tags:
    - provision
  become: yes
  block:
    - import_tasks: "tasks/reduce_privileged_ports.yml"
    - name: "Mount storage"
      block:
        - name: "Automount the internal HDD"
          mount:
            path: /data
            src: "/dev/disk/by-uuid/{{hdd1_uuid}}"
            fstype: ext4
            state: mounted
        - name: "Install NAS client"
          apt:
            name: nfs-common
        - name: "Create NAS mountpoint"
          file:
            path: /nas
            state: directory
            group: root
        - name: "Automount the NAS 'Documents' directory"
          mount:
            path: /nas/documents
            src: "{{nas_host}}:/Documents"
            fstype: nfs
            state: mounted
        - name: "Automount the NAS 'Media' directory"
          mount:
            path: /nas/media
            src: "{{nas_host}}:/Media"
            fstype: nfs
            state: mounted
    - name: "Create '{{service_user}}' user for running services"
      user:
        name: "{{service_user}}"
        group: "{{service_group}}"
    - name: "Allow Podman containers to access host"
      import_role:
        name: firewall
      vars:
        ports: "{{expose_ports}}"
    - name: "Configure Wireguard"
      block:
        - import_tasks: "tasks/wireguard.yml"

- name: "Create service config and data directories"
  tags:
    - deploy
  become: yes
  file:
    path: "{{item}}"
    state: directory
    # TODO re-enable these ownerships when using rootless podman
    # owner: "{{service_user}}"
    # group: "{{service_group}}"
  loop:
    - "/data/duplicati"
    - "{{host_config_dir}}/nextcloud"
    - "{{host_data_dir}}/gitolite/git"
    - "{{host_data_dir}}/gitolite/keys"
    - "{{host_data_dir}}/nextcloud"
    - "{{host_data_dir}}/nextcloud-mariadb"
    - "{{host_data_dir}}/nextcloud-redis"
    - "{{host_data_dir}}/plex"
    - "{{host_data_dir}}/rproxy/certs"
    - "{{host_data_dir}}/rproxy/certs"
    - "{{host_data_dir}}/rproxy/logs"
    - "{{host_data_dir}}/unifi"
    # TODO use $XDG_RUNTIME_DIR instead of /run when using rootless podman
    - "/run/plex"
- name: "Configure services"
  tags:
    - deploy
    - update
  become: yes
  block:
    - import_tasks: "tasks/configure_web_service"
- name: "Deploy services"
  block:
    - name: "Deploy backup service"
      block:
        - import_tasks: "tasks/deploy_web_service"
    - name: "Deploy git service"
      block:
        - import_tasks: "tasks/deploy_git_service"
    - name: "Deploy mediaserver service"
      block:
        - import_tasks: "tasks/deploy_mediaserver_service"
    - name: "Deploy torrent service"
      block:
        - import_tasks: "tasks/deploy_torrent_service"
    - name: "Deploy unifi service"
      block:
        - import_tasks: "tasks/deploy_unifi_service"
    - name: "Deploy web service"
      block:
        - import_tasks: "tasks/deploy_web_service"
